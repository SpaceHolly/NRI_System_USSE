<UserControl x:Class="Nri.AdminClient.Views.Administration.AdminFactionsOrganizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Nri.Ui.Wpf.Controls;assembly=Nri.Ui.Wpf"
             AutomationProperties.AutomationId="AdminFactions_Route">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <controls:NriPageHeader Grid.Row="0"
                                Title="Фракции и организации"
                                Subtitle="Справочник фракций и организаций"
                                StatusText="Недоступно в текущем профиле сервера"
                                AutomationProperties.AutomationId="AdminFactions_Header"
                                Margin="8,8,8,0"/>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Style="{StaticResource CardStyle}" Margin="8" Padding="22">
                <StackPanel>
                    <TextBlock Text="Фракции и организации" Style="{StaticResource SectionTitleTextStyle}" FontSize="22"/>
        <controls:NriRouteStateHost State="{Binding FactionsRouteState, Mode=OneWay}" ActiveStates="unavailable" Panel.ZIndex="10" AutomationProperties.AutomationId="AdminFactions_StateHost">
                        <controls:NriRouteStatePresenter State="{Binding FactionsRouteState}" VisibleFor="unavailable">
                    <TextBlock AutomationProperties.AutomationId="AdminFactions_UnavailableState"
                               Visibility="Visible"
                               Text="{Binding FactionsUnavailableStateMessage}"
                               Style="{StaticResource TitleTextStyle}"
                               Margin="0,12,0,0"
                               TextWrapping="Wrap"/>
                        </controls:NriRouteStatePresenter>
                    </controls:NriRouteStateHost>
                    <TextBlock AutomationProperties.AutomationId="AdminFactions_UnavailableExplanation"
                               Text="Редактор появится после подключения зарегистрированного серверного профиля."
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,8,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
