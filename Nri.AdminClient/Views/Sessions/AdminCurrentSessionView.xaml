<UserControl x:Class="Nri.AdminClient.Views.Sessions.AdminCurrentSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Nri.AdminClient.ViewModels">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </UserControl.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Текущая сессия" Style="{StaticResource SectionTitleTextStyle}"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Проверить flags" Style="{StaticResource GhostButtonStyle}" Command="{Binding RefreshFlagsCommand}"/>
                    <Button Content="Обновить" Margin="8,0,0,0" Command="{Binding LoadCommand}" IsEnabled="{Binding CanUseSession}"/>
                    <Button Content="Создать" Margin="8,0,0,0" Command="{Binding CreateCommand}" IsEnabled="{Binding CanUseSession}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.05*"/>
                <ColumnDefinition Width="1.15*"/>
                <ColumnDefinition Width="1.05*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,8,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Основное" Style="{StaticResource SectionTitleTextStyle}"/>
                        <TextBlock Text="CampaignId" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding CampaignId, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="SessionId" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding SessionId, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Название" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding SessionName, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Описание" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" MinHeight="56" TextWrapping="Wrap" AcceptsReturn="True"/>
                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <Border Style="{StaticResource SubtleCardStyle}" Margin="0,0,6,6">
                                <StackPanel>
                                    <TextBlock Text="Статус" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding StatusDisplay}"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SubtleCardStyle}" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="Режим" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding ModeDisplay}"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Button Content="Сохранить" Command="{Binding UpdateCommand}" IsEnabled="{Binding CanWriteSession}"/>
                            <Button Content="Старт" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding StartCommand}" IsEnabled="{Binding CanWriteSession}"/>
                            <Button Content="Пауза" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding PauseCommand}" IsEnabled="{Binding CanWriteSession}"/>
                            <Button Content="Продолжить" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding ResumeCommand}" IsEnabled="{Binding CanWriteSession}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Завершить" Style="{StaticResource GhostButtonStyle}" Command="{Binding CompleteCommand}" IsEnabled="{Binding CanWriteSession}"/>
                            <Button Content="Отменить" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding CancelCommand}" IsEnabled="{Binding CanWriteSession}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,8,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Сцена и активные ссылки" Style="{StaticResource SectionTitleTextStyle}"/>
                        <UniformGrid Columns="2" Margin="0,8,0,8">
                            <Border Style="{StaticResource SummaryMetricCardStyle}" Margin="0,0,6,6">
                                <StackPanel>
                                    <TextBlock Text="Сцена" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding SceneSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SummaryMetricCardStyle}" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="Карта сцены" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding SceneMapSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SummaryMetricCardStyle}" Margin="0,0,6,6">
                                <StackPanel>
                                    <TextBlock Text="Бой" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding CombatSummary}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                            <Border Style="{StaticResource SummaryMetricCardStyle}" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="Игрокам" Style="{StaticResource TitleTextStyle}"/>
                                    <TextBlock Text="{Binding IsPlayerVisible}"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <TextBlock Text="CurrentSceneId" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding CurrentSceneId, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Название сцены" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding CurrentSceneName, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Сохранить сцену" Margin="0,8,0,0" Command="{Binding SetSceneCommand}" IsEnabled="{Binding CanWriteSession}"/>

                        <TextBlock Text="Режим сессии" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,2"/>
                        <ComboBox ItemsSource="{Binding ModeOptions}" SelectedItem="{Binding Mode, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Сменить режим" Margin="0,8,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding SetModeCommand}" IsEnabled="{Binding CanWriteSession}"/>

                        <TextBlock Text="Scene MapId" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,2"/>
                        <TextBox Text="{Binding ActiveSceneMapId, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Назначить активную карту сцены" Margin="0,8,0,0" Command="{Binding SetActiveSceneMapCommand}" IsEnabled="{Binding CanWriteSession}"/>

                        <TextBlock Text="Combat EncounterId" Foreground="{StaticResource MutedBrush}" Margin="0,12,0,2"/>
                        <TextBox Text="{Binding ActiveCombatEncounterId, UpdateSourceTrigger=PropertyChanged}"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Назначить бой" Command="{Binding SetActiveCombatCommand}" IsEnabled="{Binding CanWriteSession}"/>
                            <Button Content="Снять бой" Margin="8,0,0,0" Style="{StaticResource GhostButtonStyle}" Command="{Binding ClearActiveCombatCommand}" IsEnabled="{Binding CanWriteSession}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardStyle}" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="Заметки и быстрые переходы" Style="{StaticResource SectionTitleTextStyle}"/>
                        <CheckBox Content="Сессия видна игрокам" IsChecked="{Binding IsPlayerVisible}" Margin="0,8,0,0"/>
                        <TextBlock Text="VisibilityMode" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding VisibilityMode, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Публичные заметки" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding PublicNotes, UpdateSourceTrigger=PropertyChanged}" MinHeight="70" TextWrapping="Wrap" AcceptsReturn="True"/>
                        <TextBlock Text="Заметки GM" Foreground="{StaticResource MutedBrush}" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding GMNotes, UpdateSourceTrigger=PropertyChanged}" MinHeight="70" TextWrapping="Wrap" AcceptsReturn="True"/>
                        <Button Content="Сохранить заметки" Margin="0,8,0,10" Command="{Binding SaveNotesCommand}" IsEnabled="{Binding CanWriteSession}"/>
                    </StackPanel>
                    <ListBox Grid.Row="1" ItemsSource="{Binding QuickLinks}">
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type vm:SessionQuickLinkVm}">
                                <Border Style="{StaticResource SubtleCardStyle}" Margin="0,0,0,6" Padding="8">
                                    <StackPanel>
                                        <DockPanel>
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                            <TextBlock DockPanel.Dock="Right" Text="{Binding StateText}" Foreground="{StaticResource MutedBrush}"/>
                                        </DockPanel>
                                        <TextBlock Text="{Binding TargetId}" Foreground="{StaticResource MutedBrush}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="8" Margin="0,8,0,0">
            <DockPanel>
                <TextBlock Text="{Binding LastRefreshText}" Foreground="{StaticResource MutedBrush}" DockPanel.Dock="Right"/>
                <StackPanel>
                    <TextBlock Text="{Binding WarningMessage}" Foreground="#FFFBBF24" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFFCA5A5" TextWrapping="Wrap"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
