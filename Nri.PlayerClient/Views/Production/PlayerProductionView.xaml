<UserControl x:Class="Nri.PlayerClient.Views.Production.PlayerProductionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right" Content="Обновить" Command="{Binding RefreshCommand}" Margin="8,0,0,0"/>
                <StackPanel>
                    <TextBlock Text="Производство / заказы" Style="{StaticResource SectionTitleTextStyle}"/>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    <TextBlock Text="Игрок видит только раскрытые GM мощности, свои оценки, свои заказы, видимые производственные проекты и готовые активы."
                               Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Доступные мощности, оценки и заказы" Style="{StaticResource TitleTextStyle}"/>
                    <TabControl Grid.Row="1" Margin="0,8,0,0">
                        <TabItem Header="Мощности">
                            <ListBox ItemsSource="{Binding Facilities}" SelectedItem="{Binding SelectedFacility, Mode=TwoWay}" DisplayMemberPath="Summary"/>
                        </TabItem>
                        <TabItem Header="Оценки">
                            <ListBox ItemsSource="{Binding Quotes}" SelectedItem="{Binding SelectedQuote, Mode=TwoWay}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2">
                                            <TextBlock Text="{Binding Summary}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </TabItem>
                        <TabItem Header="Заказы">
                            <ListBox ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder, Mode=TwoWay}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="2">
                                            <TextBlock Text="{Binding Summary}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </TabItem>
                    </TabControl>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Моё производство" Style="{StaticResource TitleTextStyle}"/>
                    <Grid Grid.Row="1" Margin="0,8,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.2*"/>
                        </Grid.ColumnDefinitions>
                        <ListBox ItemsSource="{Binding ManufacturingProjects}" SelectedItem="{Binding SelectedManufacturingProject, Mode=TwoWay}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="2">
                                        <TextBlock Text="{Binding Summary}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Secondary}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TabControl Grid.Column="1" Margin="8,0,0,0">
                            <TabItem Header="Стадии">
                                <ListBox ItemsSource="{Binding Stages}" DisplayMemberPath="Summary"/>
                            </TabItem>
                            <TabItem Header="Ресурсы">
                                <ListBox ItemsSource="{Binding Resources}" DisplayMemberPath="Summary"/>
                            </TabItem>
                            <TabItem Header="Платежи">
                                <ListBox ItemsSource="{Binding Payments}" DisplayMemberPath="Summary"/>
                            </TabItem>
                            <TabItem Header="Испытания">
                                <ListBox ItemsSource="{Binding Tests}" DisplayMemberPath="Summary"/>
                            </TabItem>
                            <TabItem Header="Замечания">
                                <ListBox ItemsSource="{Binding Defects}" DisplayMemberPath="Summary"/>
                            </TabItem>
                            <TabItem Header="Активы">
                                <ListBox ItemsSource="{Binding Assets}" DisplayMemberPath="Summary"/>
                            </TabItem>
                        </TabControl>
                    </Grid>
                    <WrapPanel Grid.Row="2">
                        <Button Content="Запросить прогресс" Command="{Binding RequestManufacturingProgressCommand}"/>
                        <Button Content="Запросить приёмку" Command="{Binding RequestAcceptanceCommand}" Margin="6,0,0,0"/>
                        <Button Content="Запросить передачу техники" Command="{Binding RequestTransferCommand}" Margin="6,0,0,0"/>
                    </WrapPanel>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,8,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*"/>
                    <ColumnDefinition Width="1.25*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Заявка GM" Style="{StaticResource TitleTextStyle}"/>
                    <TextBox Text="{Binding RequestSummary, UpdateSourceTrigger=PropertyChanged}" ToolTip="Кратко: что нужно оценить или заказать"/>
                    <TextBox Text="{Binding RequestDetails, UpdateSourceTrigger=PropertyChanged}"
                             MinHeight="56"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             ToolTip="Комментарий для GM"
                             Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock Text="Связь с чертежом / пресетом" Style="{StaticResource TitleTextStyle}"/>
                    <WrapPanel>
                        <TextBox Width="170" Text="{Binding FacilityId, UpdateSourceTrigger=PropertyChanged}" ToolTip="FacilityId"/>
                        <TextBox Width="170" Text="{Binding BlueprintId, UpdateSourceTrigger=PropertyChanged}" ToolTip="BlueprintId" Margin="6,0,0,0"/>
                        <TextBox Width="150" Text="{Binding PresetId, UpdateSourceTrigger=PropertyChanged}" ToolTip="PresetId" Margin="6,0,0,0"/>
                    </WrapPanel>
                    <TextBlock Text="Если поле неизвестно, оставьте его пустым: GM свяжет заявку с нужной мощностью или чертежом."
                               Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="8,0,0,0" VerticalAlignment="Bottom">
                    <Button Content="Запросить оценку" Command="{Binding RequestQuoteCommand}"/>
                    <Button Content="Запросить заказ" Command="{Binding RequestOrderCommand}" Margin="0,6,0,0"/>
                    <Button Content="Принять оценку" Command="{Binding AcceptQuoteCommand}" Margin="0,6,0,0"/>
                    <Button Content="Отклонить оценку" Command="{Binding RejectQuoteCommand}" Margin="0,6,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="0,8,0,0">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right" Content="Скрыть ошибку" Command="{Binding ClearErrorCommand}" Margin="8,0,0,0"/>
                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" TextWrapping="Wrap"/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
